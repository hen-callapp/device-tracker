# Device Tracker (app shell)

Single self-contained `index.html` served via GitHub Pages. Contains **no data**: all state lives in the private CallHub repo and is reachable only with an authorized token via the GitHub API.

Docs, glossary, ADRs, and the data file: `CallHub/apps/device-tracker/`.
